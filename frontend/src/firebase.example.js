// Example Firebase configuration (DO NOT commit real secrets)
// Copy values into `frontend/.env` (or your preferred env mechanism) as VITE_* variables.

// Example (placeholder values):
export const EXAMPLE_FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
  measurementId: "YOUR_MEASUREMENT_ID",
};

// Example usage (in code keep using `frontend/src/firebase.js` which reads from env):
// import { EXAMPLE_FIREBASE_CONFIG } from './firebase.example'
// console.log(EXAMPLE_FIREBASE_CONFIG)
